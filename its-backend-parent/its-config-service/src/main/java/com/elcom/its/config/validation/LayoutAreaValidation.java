package com.elcom.its.config.validation;

import com.elcom.its.config.model.dto.LayoutAreaDTO;

public class LayoutAreaValidation extends AbstractValidation {

    public String validateInsertLayoutArea(LayoutAreaDTO layoutAreaDto) {

//        if (StringUtil.isNullOrEmpty(cameraLayoutDto.getCameraId())) {
//            getMessageDes().add("Camera không được để trống");
//        }
//
//        return !isValid() ? this.buildValidationMessage() : null;
        return null;
    }
}
